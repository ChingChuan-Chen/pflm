Installation
============

We recommend a dedicated environment.

.. code-block:: shell

   pip install git+https://github.com/ChingChuan-Chen/pflm.git

If C/Cython extensions are not yet compiled, pip will build them via meson-python.