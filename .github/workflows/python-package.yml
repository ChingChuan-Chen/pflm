name: Python CI/CD

on:
  push:
    branches: [ "main" ]
    tags: [ "*" ]
  pull_request:
    branches: [ "main" ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  ruff:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5

    - name: Set up Python 3.13
      uses: actions/setup-python@v6
      with:
        python-version: "3.13"

    - name: Install ruff
      run: |
        python -m pip install --upgrade pip
        python -m pip install ruff>=0.11.7

    - name: Ruff check
      run: |
        ruff check pflm

  ubuntu-tests:
    needs: ruff
    uses: ./.github/workflows/_test-ubuntu.yml
    with:
      python-versions: '["3.12", "3.13"]'

  ubuntu-tests-extended:
    needs: ruff
    if: startsWith(github.ref, 'refs/tags/')
    uses: ./.github/workflows/_test-ubuntu.yml
    with:
      python-versions: '["3.10", "3.11"]'

  windows-conda-tests:
    needs: ruff
    uses: ./.github/workflows/_test-windows-conda.yml
    with:
      python-versions: '["3.12", "3.13"]'

  windows-conda-tests-extended:
    needs: ruff
    if: startsWith(github.ref, 'refs/tags/')
    uses: ./.github/workflows/_test-windows-conda.yml
    with:
      python-versions: '["3.10", "3.11"]'

  build-linux:
    if: startsWith(github.ref, 'refs/tags/')
    needs: [ ubuntu-tests, ubuntu-tests-extended ]
    uses: ./.github/workflows/_build-linux.yml
    with:
      python-versions: '["3.10", "3.11", "3.12", "3.13"]'

  build-windows-conda:
    if: startsWith(github.ref, 'refs/tags/')
    needs: [ windows-conda-tests, windows-conda-tests-extended ]
    uses: ./.github/workflows/_build-windows-conda.yml
    with:
      python-versions: '["3.10", "3.11", "3.12", "3.13"]'

  publish-to-pypi:
    name: "Publish Python distribution to PyPI"
    if: startsWith(github.ref, 'refs/tags/')
    needs: [ build-linux, build-windows-conda ]
    runs-on: ubuntu-latest

    environment:
      name: pypi
      url: https://pypi.org/project/pflm/

    permissions:
      id-token: write  # IMPORTANT: mandatory for trusted publishing

    steps:
    - name: Download all the dists
      uses: actions/download-artifact@v4
      with:
        pattern: python-package-distributions-*
        merge-multiple: true
        path: dist/

    - name: Publish distribution to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1

  publish-to-testpypi:
    name: "Publish Python distribution to TestPyPI"
    needs: [ build-linux, build-windows-conda ]
    runs-on: ubuntu-latest

    environment:
      name: testpypi
      url: https://test.pypi.org/project/pflm/

    permissions:
      id-token: write  # IMPORTANT: mandatory for trusted publishing

    steps:
    - name: Download all the dists
      uses: actions/download-artifact@v4
      with:
        pattern: python-package-distributions-*
        merge-multiple: true
        path: dist/

    - name: Publish distribution to TestPyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        repository-url: https://test.pypi.org/legacy/
